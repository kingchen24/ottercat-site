services:
  - type: web
    name: ottercat-site
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    plan: free
    disk:
      name: data
      mountPath: /opt/render/project/src/data
      sizeGB: 1
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
      - key: PORT
        value: "10000"
